import React from "react";

const History = () => (
  <div className="container mt-5">
    <h2>History Page</h2>
    <p>This is the user history section (e.g. past payments, actions, etc).</p>
  </div>
);

export default History;
